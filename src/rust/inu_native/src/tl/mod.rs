//! TL values as a plugin sees them: the lazy proxy over a host handle, and the two pure
//! prelude surfaces built over one raw value each.

pub(crate) mod message;
pub(crate) mod proxy;
pub(crate) mod utils;
