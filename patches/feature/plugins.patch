From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.pet>
Date: Wed, 2 Sep 2026 05:51:47 +0400
Subject: Add the QuickJS plugin engine (hooks, action rows)

---
 .../messenger/MessagesController.java         | 122 ++++++++++--------
 .../messenger/SendMessagesHelper.java         |  17 ++-
 .../telegram/tgnet/ConnectionsManager.java    |   8 +-
 .../java/org/telegram/ui/ChatActivity.java    |   6 +-
 .../java/org/telegram/ui/LaunchActivity.java  |   1 +
 5 files changed, 97 insertions(+), 57 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MessagesController.java
@@ -16800,7 +16800,10 @@ public class MessagesController extends BaseController implements NotificationCe
                         }
                     }
 
-                    Utilities.stageQueue.postRunnable(() -> {
+                    Utilities.stageQueue.postRunnable(new Runnable() { public void run() {
+                        if (desu.inugram.helpers.plugins.telegram.PluginUpdates.onDifference(res.new_messages, res.other_updates, currentAccount, this)) {
+                            return;
+                        }
                         boolean notifyAbountDifference = true;
                         if (res instanceof TLRPC.TL_updates_channelDifference || res instanceof TLRPC.TL_updates_channelDifferenceEmpty) {
                             if (!res.new_messages.isEmpty()) {
@@ -16911,7 +16914,7 @@ public class MessagesController extends BaseController implements NotificationCe
                                 NotificationCenter.getInstance(currentAccount).postNotificationName(NotificationCenter.onReceivedChannelDifference, channelId);
                             });
                         }
-                    });
+                    }});
                 });
             } else if (error != null) {
                 AndroidUtilities.runOnUIThread(() -> {
@@ -17058,7 +17061,10 @@ public class MessagesController extends BaseController implements NotificationCe
                             }
                         }
 
-                        Utilities.stageQueue.postRunnable(() -> {
+                        Utilities.stageQueue.postRunnable(new Runnable() { public void run() {
+                            if (desu.inugram.helpers.plugins.telegram.PluginUpdates.onDifference(res.new_messages, res.other_updates, currentAccount, this)) {
+                                return;
+                            }
                             if (!res.new_messages.isEmpty() || !res.new_encrypted_messages.isEmpty()) {
                                 LongSparseArray<ArrayList<MessageObject>> messages = new LongSparseArray<>();
                                 for (int b = 0; b < res.new_encrypted_messages.size(); b++) {
@@ -17176,7 +17182,7 @@ public class MessagesController extends BaseController implements NotificationCe
                             if (BuildVars.LOGS_ENABLED) {
                                 FileLog.d("received difference with date = " + getMessagesStorage().getLastDateValue() + " pts = " + getMessagesStorage().getLastPtsValue() + " seq = " + getMessagesStorage().getLastSeqValue() + " messages = " + res.new_messages.size() + " users = " + res.users.size() + " chats = " + res.chats.size() + " other updates = " + res.other_updates.size());
                             }
-                        });
+                        }});
                     });
                 }
             } else {
@@ -17926,8 +17932,64 @@ public class MessagesController extends BaseController implements NotificationCe
         }
     }
 
+    public TLRPC.TL_message inu_buildShortMessage(TLRPC.Updates updates) {
+        long userId = updates instanceof TLRPC.TL_updateShortChatMessage ? updates.from_id : updates.user_id;
+        TLRPC.TL_message message = new TLRPC.TL_message();
+        message.id = updates.id;
+        long clientUserId = getUserConfig().getClientUserId();
+        if (updates instanceof TLRPC.TL_updateShortMessage) {
+            message.from_id = new TLRPC.TL_peerUser();
+            if (updates.out) {
+                message.from_id.user_id = clientUserId;
+            } else {
+                message.from_id.user_id = userId;
+            }
+            message.peer_id = new TLRPC.TL_peerUser();
+            message.peer_id.user_id = userId;
+            message.dialog_id = userId;
+        } else {
+            message.from_id = new TLRPC.TL_peerUser();
+            message.from_id.user_id = userId;
+            message.peer_id = new TLRPC.TL_peerChat();
+            message.peer_id.chat_id = updates.chat_id;
+            message.dialog_id = -updates.chat_id;
+        }
+
+        message.fwd_from = updates.fwd_from;
+        message.silent = updates.silent;
+        message.out = updates.out;
+        message.mentioned = updates.mentioned;
+        message.media_unread = updates.media_unread;
+        message.entities = updates.entities;
+        message.message = updates.message;
+        message.date = updates.date;
+        message.via_bot_id = updates.via_bot_id;
+        message.flags = updates.flags | TLRPC.MESSAGE_FLAG_HAS_FROM_ID;
+        message.reply_to = updates.reply_to;
+        message.ttl_period = updates.ttl_period;
+        message.media = new TLRPC.TL_messageMediaEmpty();
+
+        ConcurrentHashMap<Long, Integer> read_max = message.out ? dialogs_read_outbox_max : dialogs_read_inbox_max;
+        Integer value = read_max.get(message.dialog_id);
+        if (value == null) {
+            value = getMessagesStorage().getDialogReadMax(message.out, message.dialog_id);
+            read_max.put(message.dialog_id, value);
+        }
+        message.unread = value < message.id;
+
+        if (message.dialog_id == clientUserId) {
+            message.unread = false;
+            message.media_unread = false;
+            message.out = true;
+        }
+        return message;
+    }
+
     // must be run from Utilities.stageQueue
     public void processUpdates(final TLRPC.Updates updates, boolean fromQueue) {
+        if (desu.inugram.helpers.plugins.telegram.PluginUpdates.onUpdates(this, updates, currentAccount, fromQueue)) {
+            return;
+        }
         ArrayList<Long> needGetChannelsDiff = null;
         boolean needGetDiff = false;
         boolean needReceivedQueue = false;
@@ -18028,54 +18090,7 @@ public class MessagesController extends BaseController implements NotificationCe
                 needGetDiff = true;
             } else {
                 if (getMessagesStorage().getLastPtsValue() + updates.pts_count == updates.pts) {
-                    TLRPC.TL_message message = new TLRPC.TL_message();
-                    message.id = updates.id;
-                    long clientUserId = getUserConfig().getClientUserId();
-                    if (updates instanceof TLRPC.TL_updateShortMessage) {
-                        message.from_id = new TLRPC.TL_peerUser();
-                        if (updates.out) {
-                            message.from_id.user_id = clientUserId;
-                        } else {
-                            message.from_id.user_id = userId;
-                        }
-                        message.peer_id = new TLRPC.TL_peerUser();
-                        message.peer_id.user_id = userId;
-                        message.dialog_id = userId;
-                    } else {
-                        message.from_id = new TLRPC.TL_peerUser();
-                        message.from_id.user_id = userId;
-                        message.peer_id = new TLRPC.TL_peerChat();
-                        message.peer_id.chat_id = updates.chat_id;
-                        message.dialog_id = -updates.chat_id;
-                    }
-
-                    message.fwd_from = updates.fwd_from;
-                    message.silent = updates.silent;
-                    message.out = updates.out;
-                    message.mentioned = updates.mentioned;
-                    message.media_unread = updates.media_unread;
-                    message.entities = updates.entities;
-                    message.message = updates.message;
-                    message.date = updates.date;
-                    message.via_bot_id = updates.via_bot_id;
-                    message.flags = updates.flags | TLRPC.MESSAGE_FLAG_HAS_FROM_ID;
-                    message.reply_to = updates.reply_to;
-                    message.ttl_period = updates.ttl_period;
-                    message.media = new TLRPC.TL_messageMediaEmpty();
-
-                    ConcurrentHashMap<Long, Integer> read_max = message.out ? dialogs_read_outbox_max : dialogs_read_inbox_max;
-                    Integer value = read_max.get(message.dialog_id);
-                    if (value == null) {
-                        value = getMessagesStorage().getDialogReadMax(message.out, message.dialog_id);
-                        read_max.put(message.dialog_id, value);
-                    }
-                    message.unread = value < message.id;
-
-                    if (message.dialog_id == clientUserId) {
-                        message.unread = false;
-                        message.media_unread = false;
-                        message.out = true;
-                    }
+                    TLRPC.TL_message message = inu_buildShortMessage(updates);
 
                     getMessagesStorage().setLastPtsValue(updates.pts);
                     boolean isDialogCreated = createdDialogIds.contains(message.dialog_id);
@@ -18554,6 +18569,9 @@ public class MessagesController extends BaseController implements NotificationCe
                 FileLog.d("process update " + baseUpdate.getClass().getSimpleName());
             }
             desu.inugram.InuHooks.onUpdate(baseUpdate, currentAccount);
+            if (desu.inugram.helpers.plugins.telegram.PluginUpdates.isDropped(baseUpdate)) {
+                continue;
+            }
             if (baseUpdate instanceof TL_update.TL_updateNewMessage || baseUpdate instanceof TL_update.TL_updateNewChannelMessage || baseUpdate instanceof TL_update.TL_updateNewScheduledMessage) {
                 TLRPC.Message message;
                 if (baseUpdate instanceof TL_update.TL_updateNewMessage) {
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java b/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
@@ -1766,7 +1766,7 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
         return true;
     }
 
-    protected void processSentMessage(int id) {
+    public void processSentMessage(int id) {
         int prevSize = unsentMessages.size();
         unsentMessages.remove(id);
         if (prevSize != 0 && unsentMessages.size() == 0) {
@@ -7434,7 +7434,7 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
         }
     }
 
-    protected TLRPC.Message removeFromSendingMessages(int mid, boolean scheduled) {
+    public TLRPC.Message removeFromSendingMessages(int mid, boolean scheduled) {
         TLRPC.Message message;
         if (mid > 0) {
             message = editingMessages.get(mid);
@@ -7565,6 +7565,7 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
             return;
         }
 
+        desu.inugram.helpers.plugins.telegram.PluginRpc.bindOptimisticMessages(request, currentAccount, msgObjs);
         int reqId = getConnectionsManager().sendRequest(request, (response, error) -> {
             if (error != null && FileRefController.isFileRefError(error.text)) {
                 final int fileRefIndex = FileRefController.getFileRefErrorIndex(error.text);
@@ -7835,6 +7836,9 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                     }
                     Utilities.stageQueue.postRunnable(() -> getMessagesController().processUpdates(updates, false));
                 } else {
+                    if (desu.inugram.helpers.plugins.telegram.PluginRpc.handleDroppedSends(this, currentAccount, msgObjs, scheduled, error)) {
+                        return;
+                    }
                     AlertsCreator.processError(currentAccount, error, null, request);
                     isSentError = true;
                 }
@@ -7959,6 +7963,7 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
             return;
         }
 
+        desu.inugram.helpers.plugins.telegram.PluginRpc.bindOptimisticMessage(req, currentAccount, msgObj);
         newMsgObj.reqId = getConnectionsManager().sendRequest(req, (response, error) -> {
             if (error != null && (req instanceof TLRPC.TL_messages_sendMedia || req instanceof TL_ephemeral.TL_sendMessage || req instanceof TLRPC.TL_messages_editMessage || req instanceof TLRPC.TL_messages_addPollAnswer) && FileRefController.isFileRefError(error.text)) {
                 if (FileRefController.isFileRefErrorCover(error.text)) {
@@ -8077,6 +8082,11 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                         });
 
                     } else {
+                        if (desu.inugram.helpers.plugins.telegram.PluginRpc.isDroppedSendError(error)) {
+                            removeFromSendingMessages(newMsgObj.id, scheduled);
+                            revertEditingMessageObject(msgObj);
+                            return;
+                        }
                         AlertsCreator.processError(currentAccount, error, null, req);
                         removeFromSendingMessages(newMsgObj.id, scheduled);
                         revertEditingMessageObject(msgObj);
@@ -8380,6 +8390,9 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                             }
                         }
                     } else {
+                        if (desu.inugram.helpers.plugins.telegram.PluginRpc.handleDroppedSend(this, currentAccount, newMsgObj, scheduled, error)) {
+                            return;
+                        }
                         AlertsCreator.processError(currentAccount, error, null, req);
                         isSentError = true;
                     }
diff --git a/TMessagesProj/src/main/java/org/telegram/tgnet/ConnectionsManager.java b/TMessagesProj/src/main/java/org/telegram/tgnet/ConnectionsManager.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/tgnet/ConnectionsManager.java
+++ b/TMessagesProj/src/main/java/org/telegram/tgnet/ConnectionsManager.java
@@ -381,7 +381,10 @@ public class ConnectionsManager extends BaseController {
         return requestToken;
     }
 
-    private void sendRequestInternal(TLObject object, RequestDelegate onComplete, RequestDelegateTimestamp onCompleteTimestamp, QuickAckDelegate onQuickAck, WriteToSocketDelegate onWriteToSocket, int flags, int datacenterId, int connectionType, boolean immediate, int requestToken) {
+    public void sendRequestInternal(TLObject object, RequestDelegate onComplete, RequestDelegateTimestamp onCompleteTimestamp, QuickAckDelegate onQuickAck, WriteToSocketDelegate onWriteToSocket, int flags, int datacenterId, int connectionType, boolean immediate, int requestToken) {
+        if (desu.inugram.helpers.plugins.telegram.PluginRpc.maybeIntercept(this, object, onComplete, onCompleteTimestamp, onQuickAck, onWriteToSocket, flags, datacenterId, connectionType, immediate, requestToken, currentAccount)) {
+            return;
+        }
         if (BuildVars.LOGS_ENABLED) {
             FileLog.d("send request " + object + " with token = " + requestToken);
         }
@@ -566,6 +569,7 @@ public class ConnectionsManager extends BaseController {
 
     public void cancelRequest(int token, boolean notifyServer, Runnable onCancelled) {
         Utilities.stageQueue.postRunnable(() -> {
+            desu.inugram.helpers.plugins.telegram.PluginRpc.onRequestCancelled(currentAccount, token, notifyServer, onCancelled);
             if (onCancelled != null) {
                 listenCancel(token, () -> {
                     Utilities.stageQueue.postRunnable(onCancelled);
@@ -581,6 +585,7 @@ public class ConnectionsManager extends BaseController {
 
     public void cancelRequestsForGuid(int guid) {
         Utilities.stageQueue.postRunnable(() -> {
+            desu.inugram.helpers.plugins.telegram.PluginRpc.onRequestsCancelledForGuid(currentAccount, guid);
             native_cancelRequestsForGuid(currentAccount, guid);
         });
     }
@@ -589,6 +594,7 @@ public class ConnectionsManager extends BaseController {
         if (guid == 0) {
             return;
         }
+        desu.inugram.helpers.plugins.telegram.PluginRpc.onRequestBoundToGuid(currentAccount, requestToken, guid);
         native_bindRequestToGuid(currentAccount, requestToken, guid);
     }
 
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -32205,6 +32205,7 @@ public class ChatActivity extends BaseFragment implements
                         }
                     }
                     scrimPopupWindowItems[a] = cell;
+                    ChatHelper.bindMenuCell(cell, option);
                     if (option == OPTION_REMOVE_ADS) {
                         popupLayout.addView(new ActionBarPopupWindow.GapView(contentView.getContext(), themeDelegate), LayoutHelper.createLinear(LayoutHelper.MATCH_PARENT, 8));
                     }
@@ -32644,10 +32645,11 @@ public class ChatActivity extends BaseFragment implements
                     }
                 }, 420);
             };
+            Runnable inu_showMenu = ChatHelper.gateMessageMenu(showMenu);
             if (waitForLangDetection.get()) {
-                onLangDetectionDone.set(showMenu);
+                onLangDetectionDone.set(inu_showMenu);
             } else {
-                showMenu.run();
+                inu_showMenu.run();
             }
             chatListView.stopScroll();
             chatLayoutManager.setCanScrollVertically(false);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/LaunchActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/LaunchActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/LaunchActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/LaunchActivity.java
@@ -561,6 +561,7 @@ public class LaunchActivity extends BasePermissionsActivity implements INavigati
             if (getLastFragment() != null && getLastFragment().getLastStoryViewer() != null) {
                 getLastFragment().getLastStoryViewer().updatePlayingMode();
             }
+            desu.inugram.helpers.plugins.ui.PluginScreens.onFragmentStackChanged(this);
         });
         actionBarLayout.setDelegate(this);
         Theme.loadWallpaper(true);
