From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Wed, 10 Jun 2026 01:14:57 +0400
Subject: "original" toggle in video quality selector to skip re-encoding

---
 .../telegram/messenger/MediaController.java   |  2 +-
 .../messenger/SendMessagesHelper.java         |  6 ++
 .../telegram/messenger/VideoEditedInfo.java   | 16 ++++-
 .../video/MediaCodecVideoConvertor.java       | 24 +++++---
 .../ui/Components/VideoCompressButton.java    |  8 +++
 .../java/org/telegram/ui/PhotoViewer.java     | 58 +++++++++++++++----
 6 files changed, 93 insertions(+), 21 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java b/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/MediaController.java
@@ -6796,7 +6796,7 @@ public class MediaController implements AudioManager.OnAudioFocusChangeListener,
      * Some encoders(e.g. OMX.Exynos) can forcibly raise bitrate during encoder initialization.
      */
     public static int extractRealEncoderBitrate(int width, int height, int bitrate, boolean tryHevc) {
-        String cacheKey = width + "" + height + "" + bitrate;
+        String cacheKey = width + "" + height + "" + bitrate + tryHevc;
         Integer cachedBitrate = cachedEncoderBitrates.get(cacheKey);
         if (cachedBitrate != null) return cachedBitrate;
         try {
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java b/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/SendMessagesHelper.java
@@ -11059,6 +11059,9 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                                 if (videoEditedInfo.resultWidth != videoEditedInfo.originalWidth) {
                                     originalPath += "_" + videoEditedInfo.resultWidth;
                                 }
+                                if (videoEditedInfo.inu_originalQuality) {
+                                    originalPath += "_oq";
+                                }
                                 startTime = videoEditedInfo.startTime >= 0 ? videoEditedInfo.startTime : 0;
                             }
                             TLRPC.TL_document document = null;
@@ -11902,6 +11905,9 @@ public class SendMessagesHelper extends BaseController implements NotificationCe
                         if (videoEditedInfo.resultWidth != videoEditedInfo.originalWidth) {
                             originalPath += "_" + videoEditedInfo.resultWidth;
                         }
+                        if (videoEditedInfo.inu_originalQuality) {
+                            originalPath += "_oq";
+                        }
                     }
                     startTime = videoEditedInfo.startTime >= 0 ? videoEditedInfo.startTime : 0;
                 }
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/VideoEditedInfo.java b/TMessagesProj/src/main/java/org/telegram/messenger/VideoEditedInfo.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/VideoEditedInfo.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/VideoEditedInfo.java
@@ -74,6 +74,9 @@ public class VideoEditedInfo {
     public MediaController.CropState cropState;
     public boolean isPhoto;
     public boolean isStory;
+    public boolean inu_originalQuality;
+    public boolean inu_originalQualityNeedsConvert;
+    public boolean inu_preferHevc;
     public StoryEntry.HDRInfo hdrInfo;
 
     public CollageLayout collage;
@@ -418,7 +421,7 @@ public class VideoEditedInfo {
 
     public String getString() {
         String filters;
-        if (avatarStartTime != -1 || filterState != null || paintPath != null || blurPath != null || mediaEntities != null && !mediaEntities.isEmpty() || cropState != null) {
+        if (avatarStartTime != -1 || filterState != null || paintPath != null || blurPath != null || mediaEntities != null && !mediaEntities.isEmpty() || cropState != null || inu_originalQuality) {
             int len = 10;
             if (filterState != null) {
                 len += 160;
@@ -540,6 +543,10 @@ public class VideoEditedInfo {
             } else {
                 serializedData.writeInt32(TLRPC.TL_null.constructor);
             }
+            // fork flags go past the stock v11 fields without claiming a new version: stock parsers ignore trailing bytes
+            serializedData.writeBool(inu_originalQuality);
+            serializedData.writeBool(inu_originalQualityNeedsConvert);
+            serializedData.writeBool(inu_preferHevc);
             filters = Utilities.bytesToHex(serializedData.toByteArray());
             serializedData.cleanup();
         } else {
@@ -689,6 +696,10 @@ public class VideoEditedInfo {
                                 }
                             }
                         }
+                        // absent in stock-written blobs — readBool(false) silently yields false at EOF
+                        inu_originalQuality = serializedData.readBool(false);
+                        inu_originalQualityNeedsConvert = serializedData.readBool(false);
+                        inu_preferHevc = serializedData.readBool(false);
                         serializedData.cleanup();
                     }
                 } else {
@@ -717,6 +728,9 @@ public class VideoEditedInfo {
             }
             return !mixedSoundInfos.isEmpty() || mediaEntities != null || paintPath != null || blurPath != null || filterState != null || (cropState != null && !cropState.isEmpty()) || startTime > 0 || endTime != -1 && endTime != estimatedDuration || originalHeight != resultHeight || originalWidth != resultWidth;
         }
+        if (inu_originalQuality && !inu_originalQualityNeedsConvert && !roundVideo && !isPhoto && !isSticker && !muted) {
+            return !mixedSoundInfos.isEmpty() || mediaEntities != null || paintPath != null || blurPath != null || filterState != null || cropState != null || startTime > 0 || endTime != -1 && endTime != originalDuration;
+        }
         return !mixedSoundInfos.isEmpty() || mediaEntities != null || paintPath != null || blurPath != null || filterState != null || cropState != null || !roundVideo || startTime > 0 || endTime != -1 && endTime != estimatedDuration;
     }
 
diff --git a/TMessagesProj/src/main/java/org/telegram/messenger/video/MediaCodecVideoConvertor.java b/TMessagesProj/src/main/java/org/telegram/messenger/video/MediaCodecVideoConvertor.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/messenger/video/MediaCodecVideoConvertor.java
+++ b/TMessagesProj/src/main/java/org/telegram/messenger/video/MediaCodecVideoConvertor.java
@@ -49,6 +49,7 @@ public class MediaCodecVideoConvertor {
     private static final int MEDIACODEC_TIMEOUT_DEFAULT = 2500;
     private static final int MEDIACODEC_TIMEOUT_INCREASED = 22000;
     private String outputMimeType;
+    private boolean inu_preferHevc;
 
     public boolean convertVideo(ConvertVideoParams convertVideoParams) {
         if (convertVideoParams.isSticker) {
@@ -107,7 +108,8 @@ public class MediaCodecVideoConvertor {
         String selectedEncoderName = null;
 
         final boolean isWebm = convertVideoParams.isSticker;
-        boolean shouldUseHevc = isStory;
+        inu_preferHevc = convertVideoParams.inu_preferHevc;
+        boolean shouldUseHevc = isStory || inu_preferHevc;
         outputMimeType = isWebm ? "video/x-vnd.on2.vp9" : shouldUseHevc ? "video/hevc" : "video/avc";
 
         boolean canBeBrokenEncoder = false;
@@ -164,7 +166,7 @@ public class MediaCodecVideoConvertor {
                     }
 
                     if (encoder == null) {
-                        encoder = createEncoderForMimeType();
+                        encoder = createEncoderForMimeType(resultWidth, resultHeight, 30);
                     }
 
                     MediaFormat outputFormat = MediaFormat.createVideoFormat(outputMimeType, resultWidth, resultHeight);
@@ -368,7 +370,8 @@ public class MediaCodecVideoConvertor {
                 int videoIndex = MediaController.findTrack(extractor, false);
                 int audioIndex = bitrate != -1 && !muted && volume > 0 ? MediaController.findTrack(extractor, true) : -1;
                 boolean needConvertVideo = false;
-                if (videoIndex >= 0 && !extractor.getTrackFormat(videoIndex).getString(MediaFormat.KEY_MIME).equals(MediaController.VIDEO_MIME_TYPE)) {
+                if (videoIndex >= 0 && !extractor.getTrackFormat(videoIndex).getString(MediaFormat.KEY_MIME).equals(MediaController.VIDEO_MIME_TYPE)
+                        && (!convertVideoParams.inu_preferHevc || convertVideoParams.inu_originalQualityNeedsConvert)) {
                     needConvertVideo = true;
                 }
 
@@ -455,7 +458,7 @@ public class MediaCodecVideoConvertor {
                             }
 
                             if (encoder == null) {
-                                encoder = createEncoderForMimeType();
+                                encoder = createEncoderForMimeType(w, h, framerate);
                             }
 
                             if (BuildVars.LOGS_ENABLED) {
@@ -980,14 +983,17 @@ public class MediaCodecVideoConvertor {
         }
     }
 
-    private MediaCodec createEncoderForMimeType() throws IOException {
+    private MediaCodec createEncoderForMimeType(int width, int height, int framerate) throws IOException {
         MediaCodec encoder = null;//MediaCodec.createEncoderByType(outputMimeType);
         if (outputMimeType.equals("video/hevc") && android.os.Build.VERSION.SDK_INT >= android.os.Build.VERSION_CODES.Q) {
-            String encoderName = SharedConfig.findGoodHevcEncoder();
+            String encoderName = inu_preferHevc
+                    ? desu.inugram.helpers.media.OriginalVideoQualityHelper.findHevcEncoderName(width, height, framerate)
+                    : SharedConfig.findGoodHevcEncoder();
             if (encoderName != null) {
                 encoder = MediaCodec.createByCodecName(encoderName);
             }
-        } else {
+        }
+        if (encoder == null) {
             if (outputMimeType.equals("video/hevc")) {
                 outputMimeType = "video/avc";
             }
@@ -1570,6 +1576,8 @@ public class MediaCodecVideoConvertor {
         boolean muted;
         float volume;
         boolean isStory;
+        boolean inu_preferHevc;
+        boolean inu_originalQualityNeedsConvert;
         StoryEntry.HDRInfo hdrInfo;
         public ArrayList<MixedSoundInfo> soundInfos = new ArrayList<MixedSoundInfo>();
         int account;
@@ -1623,6 +1631,8 @@ public class MediaCodecVideoConvertor {
             params.muted = info.muted;
             params.volume = info.volume;
             params.isStory = info.isStory;
+            params.inu_preferHevc = info.inu_preferHevc;
+            params.inu_originalQualityNeedsConvert = info.inu_originalQualityNeedsConvert;
             params.hdrInfo = info.hdrInfo;
             params.isDark = info.isDark;
             params.wallpaperPeerId = info.wallpaperPeerId;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/VideoCompressButton.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/VideoCompressButton.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/VideoCompressButton.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/VideoCompressButton.java
@@ -98,6 +98,14 @@ public class VideoCompressButton extends View {
         invalidate();
     }
 
+    public void inu_setOriginalState() {
+        this.disabled = false;
+        textDrawable.setText("ORIG");
+        sizeTextDrawable.setText("", true);
+        setClickable(true);
+        invalidate();
+    }
+
     public void setPhotoState(boolean highQuality) {
         this.disabled = false;
         textDrawable.setText(highQuality ? "HD" : "SD");
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java b/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/PhotoViewer.java
@@ -6147,6 +6147,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         containerView.addView(qualityPicker, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, 48, Gravity.BOTTOM | Gravity.LEFT));
         qualityPicker.cancelButton.setOnClickListener(view -> {
             selectedCompression = previousCompression;
+            inu_originalQualitySelected = inu_previousOriginalQualitySelected;
             didChangedCompressionLevel(false);
             showQualityView(false);
             requestVideoPreview(2);
@@ -6180,6 +6181,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         qualityChooseView.setVisibility(View.INVISIBLE);
         qualityChooseView.setBackgroundColor(0x7f000000);
         containerView.addView(qualityChooseView, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, 70, Gravity.LEFT | Gravity.BOTTOM, 0, 0, 0, 48));
+        desu.inugram.helpers.media.OriginalVideoQualityHelper.createRow(this, qualityChooseView, resourcesProvider);
 
         Paint pickerBackgroundPaint = new Paint();
         pickerBackgroundPaint.setColor(0x7f000000);
@@ -7628,11 +7630,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         compressItem.setBackground(Theme.createInsetRoundRectDrawable(0x10FFFFFF, dp(22), dp(4), dp(6)));
 
         selectedCompression = selectCompression();
+        if (inu_originalVideoQualityAvailable() && desu.inugram.helpers.media.OriginalVideoQualityHelper.loadPref()) compressItem.inu_setOriginalState(); else
         compressItem.setState(videoConvertSupported && compressionsCount > 1, muteVideo, Math.min(resultWidth, resultHeight));
         compressItem.setContentDescription(getString("AccDescrVideoQuality", R.string.AccDescrVideoQuality));
         itemsLayout.addView(compressItem, LayoutHelper.createLinear(48, 48));
         compressItem.setOnClickListener(v -> {
-            if (isCaptionOpen() || muteVideo) {
+            if (isCaptionOpen() || muteVideo && !inu_isOriginalVideoQuality()) {
                 return;
             }
             if (currentIndex >= 0 && currentIndex < imagesArrLocals.size()) {
@@ -9846,6 +9849,9 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         videoEditedInfo.start = videoCutStart;
         videoEditedInfo.end = videoCutEnd;
         videoEditedInfo.compressQuality = selectedCompression;
+        videoEditedInfo.inu_originalQuality = inu_isOriginalVideoQuality();
+        videoEditedInfo.inu_originalQualityNeedsConvert = videoEditedInfo.inu_originalQuality && !inu_canSendVideoAsIs();
+        videoEditedInfo.inu_preferHevc = inu_encodesToHevc();
         videoEditedInfo.rotationValue = rotationValue;
         videoEditedInfo.originalWidth = originalWidth;
         videoEditedInfo.originalHeight = originalHeight;
@@ -14992,6 +14998,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                     float start = 0.0f;
                     float end = 1.0f;
                     int compressQuality = -1;
+                    boolean originalQuality = false;
                     String coverPath = null;
                     TLRPC.Photo coverPhoto = null;
                     Object coverPhotoObject = null;
@@ -15002,13 +15009,14 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                             start = photoEntry.editedInfo.start;
                             end = photoEntry.editedInfo.end;
                             compressQuality = photoEntry.editedInfo.compressQuality;
+                            originalQuality = photoEntry.editedInfo.inu_originalQuality;
                             coverPath = photoEntry.coverPath;
                         }
                         coverPhoto = photoEntry.coverPhoto;
                         coverPhotoObject = photoEntry.coverPhotoParentObject;
                     }
                     if (sendPhotoType != SELECT_TYPE_NO_SELECT) {
-                        processOpenVideo(currentPathObject, livePhotoVideoOffset, isMuted, start, end, compressQuality, livePhotoTimestampUs);
+                        processOpenVideo(currentPathObject, livePhotoVideoOffset, isMuted, start, end, compressQuality, originalQuality, livePhotoTimestampUs);
                         if (isDocumentsPicker) {
                             showVideoTimeline(false, animated);
                             videoAvatarTooltip.setVisibility(View.GONE);
@@ -21128,6 +21136,24 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
     private volatile int selectedCompression;
     private volatile int compressionsCount = -1;
     private int previousCompression;
+    public boolean inu_originalQualitySelected;
+    private boolean inu_previousOriginalQualitySelected;
+
+    public boolean inu_originalVideoQualityAvailable() {
+        return sendPhotoType != SELECT_TYPE_AVATAR && !centerImageIsLivePhoto;
+    }
+
+    private boolean inu_isOriginalVideoQuality() {
+        return inu_originalQualitySelected && inu_originalVideoQualityAvailable();
+    }
+
+    private boolean inu_canSendVideoAsIs() {
+        return inu_isOriginalVideoQuality() && desu.inugram.helpers.media.OriginalVideoQualityHelper.canSendAsIs(currentPathObject, inu_videoCodecMime);
+    }
+
+    private boolean inu_encodesToHevc() {
+        return inu_isOriginalVideoQuality() && desu.inugram.helpers.media.OriginalVideoQualityHelper.isHevc(inu_videoCodecMime);
+    }
 
     private int rotationValue;
     private volatile int originalWidth;
@@ -21140,6 +21166,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
     private int videoFramerate;
     private volatile boolean videoConvertSupported;
     private volatile boolean isH264Video;
+    private volatile String inu_videoCodecMime;
     private long startTime;
     private long endTime;
     private float videoCutStart;
@@ -21321,7 +21348,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                     actionBarContainer.setSubtitle(getString("SoundMuted", R.string.SoundMuted));
                 }
                 muteDrawable.setMuted(true, true);
-                if (compressItem.getTag() != null) {
+                if (compressItem.getTag() != null && !inu_isOriginalVideoQuality()) {
                     compressItem.setAlpha(0.5f);
                     compressItem.setEnabled(false);
                 }
@@ -21348,11 +21375,12 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         }
     }
 
-    private void didChangedCompressionLevel(boolean request) {
+    public void didChangedCompressionLevel(boolean request) {
         SharedPreferences preferences = MessagesController.getGlobalMainSettings();
         SharedPreferences.Editor editor = preferences.edit();
         editor.putInt(String.format("compress_video_%d", compressionsCount), selectedCompression);
         editor.commit();
+        desu.inugram.helpers.media.OriginalVideoQualityHelper.savePref(inu_originalQualitySelected);
         updateWidthHeightBitrateForCompression();
         updateVideoInfo();
         if (request) {
@@ -21376,7 +21404,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                 ? (editState.croppedMediaEntities != null && !editState.croppedMediaEntities.isEmpty() ? editState.croppedMediaEntities : null)
                 : (editState.mediaEntities != null && !editState.mediaEntities.isEmpty() ? editState.mediaEntities : null);
         Object paintPath = editState.croppedPaintPath != null ? editState.croppedPaintPath : editState.paintPath;
-        return !isH264Video || videoCutStart != 0 || rotationValue != 0 || resultWidth != originalWidth || resultHeight != originalHeight
+        return !isH264Video && !inu_canSendVideoAsIs() || videoCutStart != 0 || rotationValue != 0 || resultWidth != originalWidth || resultHeight != originalHeight
                 || editState.cropState != null || mediaEntities != null || paintPath != null || editState.savedFilterState != null || sendPhotoType == SELECT_TYPE_AVATAR;
     }
 
@@ -21389,6 +21417,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
             return;
         }
         if (!centerImageIsLivePhoto) {
+            if (inu_isOriginalVideoQuality()) compressItem.inu_setOriginalState(); else
             compressItem.setState(videoConvertSupported && compressionsCount > 1, muteVideo, Math.min(resultWidth, resultHeight));
         }
         itemsLayout.requestLayout();
@@ -21440,7 +21469,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         }
     }
 
-    private void requestVideoPreview(int request) {
+    public void requestVideoPreview(int request) {
         if (videoPreviewMessageObject != null) {
             MediaController.getInstance().cancelVideoConvert(videoPreviewMessageObject);
         }
@@ -21519,7 +21548,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
     }
 
     private Size calculateResultVideoSize() {
-        if (compressionsCount == 1) {
+        if (compressionsCount == 1 || inu_isOriginalVideoQuality()) {
             return new Size(originalWidth, originalHeight);
         }
         float maxSize;
@@ -21559,10 +21588,10 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         if (bitrate != 0 && sendPhotoType != SELECT_TYPE_AVATAR) {
             Size resultSize = calculateResultVideoSize();
             if (resultSize.getWidth() == originalWidth && resultSize.getHeight() == originalHeight) {
-                MediaController.extractRealEncoderBitrate(resultSize.getWidth(), resultSize.getHeight(), originalBitrate, false);
+                MediaController.extractRealEncoderBitrate(resultSize.getWidth(), resultSize.getHeight(), originalBitrate, inu_encodesToHevc());
             } else {
                 int targetBitrate = MediaController.makeVideoBitrate(originalHeight, originalWidth, originalBitrate, resultSize.getHeight(), resultSize.getWidth());
-                MediaController.extractRealEncoderBitrate(resultSize.getWidth(), resultSize.getHeight(), targetBitrate, false);
+                MediaController.extractRealEncoderBitrate(resultSize.getWidth(), resultSize.getHeight(), targetBitrate, inu_encodesToHevc());
             }
         }
     }
@@ -21592,10 +21621,10 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                 encoderBitrate = bitrate;
             } else if (resultWidth == originalWidth && resultHeight == originalHeight) {
                 bitrate = originalBitrate;
-                encoderBitrate = MediaController.extractRealEncoderBitrate(resultWidth, resultHeight, bitrate, false);
+                encoderBitrate = MediaController.extractRealEncoderBitrate(resultWidth, resultHeight, bitrate, inu_encodesToHevc());
             } else {
                 bitrate = MediaController.makeVideoBitrate(originalHeight, originalWidth, originalBitrate, resultHeight, resultWidth);
-                encoderBitrate = MediaController.extractRealEncoderBitrate(resultWidth, resultHeight, bitrate, false);
+                encoderBitrate = MediaController.extractRealEncoderBitrate(resultWidth, resultHeight, bitrate, inu_encodesToHevc());
             }
             videoFramesSize = (long) (encoderBitrate / 8 * videoDuration / 1000);
         }
@@ -21608,6 +21637,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
 
         if (show) {
             previousCompression = selectedCompression;
+            inu_previousOriginalQualitySelected = inu_originalQualitySelected;
         }
         if (qualityChooseViewAnimation != null) {
             qualityChooseViewAnimation.cancel();
@@ -21723,7 +21753,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         return new ByteArrayInputStream(output, 0, outPos);
     }
 
-    private void processOpenVideo(final String videoPath, long videoPathOffset, boolean muted, float start, float end, final int compressQuality, long livePhotoTimestampUs) {
+    private void processOpenVideo(final String videoPath, long videoPathOffset, boolean muted, float start, float end, final int compressQuality, final boolean originalQuality, long livePhotoTimestampUs) {
         if (currentLoadingVideoRunnable != null) {
             Utilities.globalQueue.cancelRunnable(currentLoadingVideoRunnable);
             currentLoadingVideoRunnable = null;
@@ -21731,6 +21761,8 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
         videoTimelineView.setVideoPath(videoPath, videoPathOffset, start, end, livePhotoTimestampUs);
         videoPreviewMessageObject = null;
         muteVideo = muted || sendPhotoType == SELECT_TYPE_AVATAR;
+        inu_originalQualitySelected = originalQuality || compressQuality == -1 && desu.inugram.helpers.media.OriginalVideoQualityHelper.loadPref();
+        if (!centerImageIsLivePhoto && inu_isOriginalVideoQuality()) compressItem.inu_setOriginalState();
 
         compressionsCount = -1;
         rotationValue = 0;
@@ -21757,6 +21789,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                     resultHeight = originalHeight = params[AnimatedFileInfo.PARAM_NUM_HEIGHT];
                     updateCompressionsCount(originalWidth, originalHeight);
                     selectedCompression = compressQuality == -1 ? selectCompression() : compressQuality;
+                    inu_videoCodecMime = desu.inugram.helpers.media.OriginalVideoQualityHelper.getVideoCodecMime(videoPath);
                     prepareRealEncoderBitrate();
                     isH264Video = MediaController.isH264Video(videoPath);
                 }
@@ -21784,6 +21817,7 @@ public class PhotoViewer implements NotificationCenter.NotificationCenterDelegat
                         }
 
                         if (!centerImageIsLivePhoto) {
+                            if (inu_isOriginalVideoQuality()) compressItem.inu_setOriginalState(); else
                             compressItem.setState(compressionsCount > 1, muteVideo, Math.min(resultWidth, resultHeight));
                         }
                         if (BuildVars.LOGS_ENABLED) {
