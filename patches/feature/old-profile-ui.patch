From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Tue, 12 May 2026 00:00:00 +0000
Subject: Restore 11.14 profile layout (avatar pos/scale, name/lastseen pos, music view)

when old layout is enabled

Suppress ProfileGooeyView morph when old layout is enabled

Restore linear gradient on profile TopView when old layout is enabled
---
 .../ui/Components/ProfileGooeyView.java       |   1 +
 .../java/org/telegram/ui/ProfileActivity.java | 299 +++++++++++-------
 2 files changed, 189 insertions(+), 111 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/ProfileGooeyView.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/ProfileGooeyView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/ProfileGooeyView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/ProfileGooeyView.java
@@ -107,6 +107,7 @@ public class ProfileGooeyView extends FrameLayout {
     }
 
     public void setGooeyEnabled(boolean enabled) {
+        if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) return;
         if (this.enabled == enabled) {
             return;
         }
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ProfileActivity.java
@@ -345,18 +345,18 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
             PHONE_OPTION_TELEGRAM_CALL = 2,
             PHONE_OPTION_TELEGRAM_VIDEO_CALL = 3;
 
-    private RecyclerListView listView;
+    public RecyclerListView listView;
     private RecyclerListView searchListView;
     private LinearLayoutManager layoutManager;
     private ListAdapter listAdapter;
     private SearchAdapter searchAdapter;
-    private SimpleTextView[] nameTextView = new SimpleTextView[2];
+    public SimpleTextView[] nameTextView = new SimpleTextView[2];
     private String nameTextViewRightDrawableContentDescription = null;
     private String nameTextViewRightDrawable2ContentDescription = null;
-    private SimpleTextView[] onlineTextView = new SimpleTextView[4];
-    private AudioPlayerAlert.ClippingTextViewSwitcher mediaCounterTextView;
-    private RLottieImageView writeButton;
-    private AnimatorSet writeButtonAnimation;
+    public SimpleTextView[] onlineTextView = new SimpleTextView[4];
+    public AudioPlayerAlert.ClippingTextViewSwitcher mediaCounterTextView;
+    public RLottieImageView writeButton;
+    public AnimatorSet writeButtonAnimation;
     private Drawable lockIconDrawable;
     private final Drawable[] verifiedDrawable = new Drawable[2];
     private final Drawable[] premiumStarDrawable = new Drawable[2];
@@ -368,7 +368,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private final CrossfadeDrawable[] premiumCrossfadeDrawable = new CrossfadeDrawable[2];
     private ScamDrawable scamDrawable;
     private UndoView undoView;
-    private OverlaysView overlaysView;
+    public OverlaysView overlaysView;
     public SharedMediaLayout sharedMediaLayout;
     private StickerEmptyView emptyView;
     private boolean sharedMediaLayoutAttached;
@@ -383,28 +383,28 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
     private HintView fwdRestrictedHint;
 //    private ProfileMetaballView metaball;
-    private FrameLayout avatarContainer;
-    private FrameLayout avatarContainer2;
+    public FrameLayout avatarContainer;
+    public FrameLayout avatarContainer2;
     private ProfileActionsView actionsView;
     private MessagesController.SavedMusicList savedMusicList;
     private ProfileMusicView musicView;
     private AnimatedStatusView animatedStatusView;
-    private AvatarImageView avatarImage;
+    public AvatarImageView avatarImage;
     private View avatarOverlay;
     private AnimatorSet avatarAnimation;
     private RadialProgressView avatarProgressView;
-    private ImageView timeItem;
-    private ImageView starBgItem, starFgItem;
+    public ImageView timeItem;
+    public ImageView starBgItem, starFgItem;
     private TimerDrawable timerDrawable;
     private ProfileGalleryBlurView avatarsBlurView;
-    private ProfileGalleryView avatarsViewPager;
-    private PagerIndicatorView avatarsViewPagerIndicatorView;
+    public ProfileGalleryView avatarsViewPager;
+    public PagerIndicatorView avatarsViewPagerIndicatorView;
     private AvatarDrawable avatarDrawable;
-    private ImageUpdater imageUpdater;
+    public ImageUpdater imageUpdater;
     private int avatarColor;
     TimerDrawable autoDeleteItemDrawable;
     private ProfileGooeyView avatarGooey;
-    private ProfileStoriesView storyView;
+    public ProfileStoriesView storyView;
     public ProfileGiftsView giftsView;
 
     private View scrimView = null;
@@ -426,10 +426,10 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private int lastMeasuredContentWidth;
     private int lastMeasuredContentHeight;
     private int listContentHeight;
-    private boolean openingAvatar;
+    public boolean openingAvatar;
     private boolean fragmentViewAttached;
 
-    private boolean doNotSetForeground;
+    public boolean doNotSetForeground;
     public boolean hasMainTabs;
 
     private boolean[] isOnline = new boolean[1];
@@ -444,8 +444,8 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private ActionBarMenuItem callItem;
     private ActionBarMenuItem videoCallItem;
     private ActionBarMenuItem editItem;
-    private ActionBarMenuItem otherItem;
-    private ActionBarMenuItem searchItem;
+    public ActionBarMenuItem otherItem;
+    public ActionBarMenuItem searchItem;
     private ActionBarMenuSubItem editColorItem;
     private ActionBarMenuSubItem linkItem;
     private ActionBarMenuSubItem setUsernameItem;
@@ -454,9 +454,9 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     AutoDeletePopupWrapper autoDeletePopupWrapper;
     protected float headerShadowAlpha = 1.0f;
     private int actionBarBackgroundColor;
-    private TopView topView;
+    public TopView topView;
     private long userId;
-    private long chatId;
+    public long chatId;
     private long topicId;
     public boolean saved;
     private long dialogId;
@@ -477,7 +477,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     public boolean openCommonChats;
     public int initialStoryAlbum;
 
-    private boolean scrolling;
+    public boolean scrolling;
 
     private boolean canSearchMembers;
 
@@ -487,10 +487,10 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
     private long banFromGroup;
     private float backwardTransitionFromExtraHeight;
-    private boolean openAnimationInProgress;
+    public boolean openAnimationInProgress;
     private boolean transitionAnimationInProress;
     private boolean recreateMenuAfterAnimation;
-    private int playProfileAnimation;
+    public int playProfileAnimation;
     private boolean needTimerImage;
     private boolean needStarImage;
     private boolean allowProfileAnimation = true;
@@ -498,36 +498,36 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private boolean justFullyExpanded = false;
     private boolean ignoreScrollOnFullExpand = false;
 
-    private float extraHeight;
-    private float initialAnimationExtraHeight;
-    private float avatarAnimationProgress;
+    public float extraHeight;
+    public float initialAnimationExtraHeight;
+    public float avatarAnimationProgress;
 
-    private int searchTransitionOffset;
+    public int searchTransitionOffset;
     private float searchTransitionProgress;
     private Animator searchViewTransition;
-    private boolean searchMode;
+    public boolean searchMode;
 
     private FlagSecureReason flagSecure;
 
     private HashMap<Integer, Integer> positionToOffset = new HashMap<>();
 
-    private float avatarX;
-    private float avatarY;
-    private float avatarScale;
+    public float avatarX;
+    public float avatarY;
+    public float avatarScale;
     private float pullUpProgress;
-    private float nameX;
-    private float nameY;
-    private float onlineX;
-    private float onlineY;
-    private float expandProgress;
-    private float listViewVelocityY;
-    private ValueAnimator expandAnimator;
+    public float nameX;
+    public float nameY;
+    public float onlineX;
+    public float onlineY;
+    public float expandProgress;
+    public float listViewVelocityY;
+    public ValueAnimator expandAnimator;
     private float currentExpandAnimatorValue;
-    private float currentExpanAnimatorFracture;
-    private float[] expandAnimatorValues = new float[]{0f, 1f};
-    private boolean isInLandscapeMode;
-    private boolean allowPullingDown;
-    private boolean isPulledDown;
+    public float currentExpanAnimatorFracture;
+    public float[] expandAnimatorValues = new float[]{0f, 1f};
+    public boolean isInLandscapeMode;
+    public boolean allowPullingDown;
+    public boolean isPulledDown;
 
     private final Paint whitePaint = new Paint();
 
@@ -535,7 +535,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private BotLocation botLocation;
     private BotBiometry botBiometry;
 
-    private TLRPC.ChatFull chatInfo;
+    public TLRPC.ChatFull chatInfo;
     private TLRPC.UserFull userInfo;
 
     public ProfileChannelCell.ChannelMessageFetcher profileChannelMessageFetcher;
@@ -549,7 +549,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private ArrayList<Integer> sortedUsers;
 
     private TLRPC.EncryptedChat currentEncryptedChat;
-    private TLRPC.Chat currentChat;
+    public TLRPC.Chat currentChat;
     private TL_bots.BotInfo botInfo;
     private TLRPC.ChannelParticipant currentChannelParticipant;
     private TL_account.TL_password currentPassword;
@@ -574,18 +574,18 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private final static int add_member = 18;
     private final static int statistics = 19;
     private final static int start_secret_chat = 20;
-    private final static int gallery_menu_save = 21;
+    public final static int gallery_menu_save = 21;
     private final static int view_discussion = 22;
     private final static int delete_topic = 23;
     private final static int report = 24;
 
     private final static int edit_info = 30;
-    private final static int logout = 31;
+    public final static int logout = 31;
     private final static int search_button = 32;
-    private final static int set_as_main = 33;
-    private final static int edit_avatar = 34;
-    private final static int delete_avatar = 35;
-    private final static int add_photo = 36;
+    public final static int set_as_main = 33;
+    public final static int edit_avatar = 34;
+    public final static int delete_avatar = 35;
+    public final static int add_photo = 36;
     private final static int gift_premium = 38;
     private final static int channel_stories = 39;
     private final static int edit_color = 40;
@@ -603,7 +603,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
     private int rowCount;
 
-    private int setAvatarRow;
+    public int setAvatarRow;
     private int setAvatarSectionRow;
     private int channelRow;
     private int channelDividerRow;
@@ -645,7 +645,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     private int emptyRow;
     private int emptyRow2;
     private int bottomPaddingRow;
-    private int infoHeaderRow;
+    public int infoHeaderRow;
     private int infoHeaderRowEmpty;
     private int infoEndRowEmpty;
     private int phoneRow;
@@ -849,7 +849,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     };
     private boolean fragmentOpened;
     private NestedFrameLayout contentView;
-    private float titleAnimationsYDiff;
+    public float titleAnimationsYDiff;
     private float customAvatarProgress;
     private float customPhotoOffset;
     private boolean hasFallbackPhoto;
@@ -1205,7 +1205,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         }
     }
 
-    private class TopView extends FrameLayout {
+    public class TopView extends FrameLayout {
 
         private int currentColor;
         private Paint paint = new Paint();
@@ -1235,7 +1235,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
         private int backgroundGradientColor1, backgroundGradientColor2, backgroundGradientX;
         public float backgroundGradientRadius, backgroundGradientY;
-        public RadialGradient backgroundGradient;
+        public android.graphics.Shader backgroundGradient;
         public final Matrix backgroundGradientMatrix = new Matrix();
         private final Paint backgroundPaint = new Paint(Paint.ANTI_ALIAS_FLAG);
 
@@ -1335,6 +1335,14 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
             }
 
             int gradientX = getWidth() / 2;
+            if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+                if (backgroundGradient == null || backgroundGradientColor1 != color1 || backgroundGradientColor2 != color2) {
+                    int gradientHeight = AndroidUtilities.statusBarHeight + AndroidUtilities.dp(144);
+                    backgroundGradient = new android.graphics.LinearGradient(0, 0, 0, gradientHeight, new int[]{backgroundGradientColor2 = color2, backgroundGradientColor1 = color1}, new float[]{0, 1}, Shader.TileMode.CLAMP);
+                    backgroundPaint.setShader(backgroundGradient);
+                    backgroundGradientX = gradientX;
+                }
+            } else {
             if (backgroundGradient == null || backgroundGradientColor1 != color1 || backgroundGradientColor2 != color2 || backgroundGradientX != gradientX) {
                 backgroundGradientRadius = dp(96) * 2;
                 backgroundGradientY = (actionBar.getOccupyStatusBar() ? AndroidUtilities.statusBarHeight : 0)
@@ -1345,6 +1353,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 backgroundGradient.setLocalMatrix(backgroundGradientMatrix);
                 backgroundPaint.setShader(backgroundGradient);
             }
+            }
         }
 
         @Override
@@ -1413,7 +1422,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         private Rect blurBounds = new Rect();
     }
 
-    private class OverlaysView extends View implements ProfileGalleryView.Callback {
+    public class OverlaysView extends View implements ProfileGalleryView.Callback {
 
         private final int statusBarHeight = actionBar.getOccupyStatusBar() && !inBubbleMode ? AndroidUtilities.statusBarHeight : 0;
 
@@ -1843,7 +1852,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         }
     }
 
-    private class PagerIndicatorView extends View {
+    public class PagerIndicatorView extends View {
 
         private final RectF indicatorRect = new RectF();
 
@@ -3892,6 +3901,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                 }
             });
 
+            if (!desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
             actionsView = new ProfileActionsView(context, dp(74)) {
                 @Override
                 public void setTranslationY(float translationY) {
@@ -3994,6 +4004,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
                         break;
                 }
             });
+            }
         }
 
         ActionBarMenu menu = actionBar.createMenu();
@@ -6051,6 +6062,9 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     }
 
     private int getHeaderOnlyExtraHeight() {
+        if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+            return dp(88f);
+        }
         if (getActionsExtraHeight() == 0) {
             return dp(168f);
         }
@@ -6061,6 +6075,9 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         return getActionsExtraHeight(true);
     }
     private int getActionsExtraHeight(boolean withMusic) {
+        if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+            return (withMusic && hasMusic) ? dp(25) : 0;
+        }
         if (userId != 0 && imageUpdater != null && !myProfile)
             return 0;
         return dp(74 + (withMusic && hasMusic ? 25 : 0));
@@ -6350,44 +6367,85 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
         updateEmojiStatusDrawableColor(value);
 
-        float nameX = this.nameX;
-        float onlineX = this.onlineX;
-
-        if (diff < 1f) {
-            nameX = AndroidUtilities.lerp(-dpf2(42 + 21), nameX, diff);
-            onlineX = AndroidUtilities.lerp(-dpf2(42 + 21), onlineX, diff);
-        }
-        final float kx = dpf2(8);
-        final float ky = isPulledDown ? dpf2(8) : dpf2(-24);
-
-        final float nameTextViewXEnd = AndroidUtilities.dpf2(18f) - ((FrameLayout.LayoutParams) nameTextView[1].getLayoutParams()).leftMargin;
-        final float nameTextViewYEnd = newTop + extraHeight - getActionsExtraHeight() - AndroidUtilities.dpf2(30f) - nameTextView[1].getBottom();
-        final float nameTextViewCx = kx + nameX + (nameTextViewXEnd - nameX) / 2f;
-        final float nameTextViewCy = ky + nameY + (nameTextViewYEnd - nameY) / 2f;
-        final float nameTextViewX = (1 - value) * (1 - value) * nameX + 2 * (1 - value) * value * nameTextViewCx + value * value * nameTextViewXEnd;
-        final float nameTextViewY = (1 - value) * (1 - value) * nameY + 2 * (1 - value) * value * nameTextViewCy + value * value * nameTextViewYEnd;
-
-        final float onlineTextViewXEnd = AndroidUtilities.dpf2(16f) - ((FrameLayout.LayoutParams) onlineTextView[1].getLayoutParams()).leftMargin;
-        final float onlineTextViewYEnd = newTop + extraHeight - getActionsExtraHeight() - AndroidUtilities.dpf2(10f) - onlineTextView[1].getBottom();
-        final float onlineTextViewCx = kx + onlineX + (onlineTextViewXEnd - onlineX) / 2f;
-        final float onlineTextViewCy = ky + onlineY + (onlineTextViewYEnd - onlineY) / 2f;
-        final float onlineTextViewX = (1 - value) * (1 - value) * onlineX + 2 * (1 - value) * value * onlineTextViewCx + value * value * onlineTextViewXEnd;
-        final float onlineTextViewY = (1 - value) * (1 - value) * onlineY + 2 * (1 - value) * value * onlineTextViewCy + value * value * onlineTextViewYEnd;
-
-        final float minEndNameY = (actionBar.getOccupyStatusBar() ? AndroidUtilities.statusBarHeight : 0)
-                + ActionBar.getCurrentActionBarHeight() / 2f
-                - 21 * AndroidUtilities.density
-                + actionBar.getTranslationY();
+        float nameX, onlineX;
+        final float nameTextViewX, nameTextViewY, onlineTextViewX, onlineTextViewY;
+        if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+            // 11.14: refresh nameX/Y/onlineX/Y in the partial-pull-down zone so
+            // the bezier source point tracks the live avatarScale instead of
+            // remaining stuck at the value last written by needLayout. nameX and
+            // onlineX are shadowed by locals later in this method, so use this.
+            if (extraHeight > AndroidUtilities.dp(88f) && expandProgress < 0.33f) {
+                final float k0 = (42f + 18f) / 42f;
+                this.nameX = AndroidUtilities.dp(-21f) + avatarContainer.getMeasuredWidth() * (avatarScale - k0);
+                this.onlineX = AndroidUtilities.dp(-21f) + avatarContainer.getMeasuredWidth() * (avatarScale - k0);
+                nameY = (float) Math.floor(avatarY) + AndroidUtilities.dp(1.3f) + AndroidUtilities.dp(7f) + avatarContainer.getMeasuredHeight() * (avatarScale - k0) / 2f;
+                onlineY = (float) Math.floor(avatarY) + AndroidUtilities.dp(24) + (float) Math.floor(11 * AndroidUtilities.density) + avatarContainer.getMeasuredHeight() * (avatarScale - k0) / 2f;
+            }
+
+            // 11.14 bezier: 38dp/18dp anchors, no min clamp, no diff-based slide.
+            nameX = this.nameX;
+            onlineX = this.onlineX;
+            final float k = AndroidUtilities.dpf2(8f);
+            final float nameTextViewXEnd = AndroidUtilities.dpf2(18f) - nameTextView[1].getLeft();
+            final float nameTextViewYEnd = newTop + extraHeight - AndroidUtilities.dpf2(38f) - nameTextView[1].getBottom();
+            final float nameTextViewCx = k + nameX + (nameTextViewXEnd - nameX) / 2f;
+            final float nameTextViewCy = k + nameY + (nameTextViewYEnd - nameY) / 2f;
+            nameTextViewX = (1 - value) * (1 - value) * nameX + 2 * (1 - value) * value * nameTextViewCx + value * value * nameTextViewXEnd;
+            nameTextViewY = (1 - value) * (1 - value) * nameY + 2 * (1 - value) * value * nameTextViewCy + value * value * nameTextViewYEnd;
+
+            final float onlineTextViewXEnd = AndroidUtilities.dpf2(16f) - onlineTextView[1].getLeft();
+            final float onlineTextViewYEnd = newTop + extraHeight - AndroidUtilities.dpf2(18f) - onlineTextView[1].getBottom();
+            final float onlineTextViewCx = k + onlineX + (onlineTextViewXEnd - onlineX) / 2f;
+            final float onlineTextViewCy = k + onlineY + (onlineTextViewYEnd - onlineY) / 2f;
+            onlineTextViewX = (1 - value) * (1 - value) * onlineX + 2 * (1 - value) * value * onlineTextViewCx + value * value * onlineTextViewXEnd;
+            onlineTextViewY = (1 - value) * (1 - value) * onlineY + 2 * (1 - value) * value * onlineTextViewCy + value * value * onlineTextViewYEnd;
+
+            nameTextView[1].setTranslationX(nameTextViewX);
+            nameTextView[1].setTranslationY(nameTextViewY);
+            onlineTextView[1].setTranslationX(getOnlineTextViewTranslationXWithOffsets(onlineTextViewX));
+            onlineTextView[1].setTranslationY(getOnlineTextViewTranslationYWithOffsets(onlineTextViewY));
+            mediaCounterTextView.setTranslationX(onlineTextViewX);
+            mediaCounterTextView.setTranslationY(onlineTextViewY);
+        } else {
+            nameX = this.nameX;
+            onlineX = this.onlineX;
+
+            if (diff < 1f) {
+                nameX = AndroidUtilities.lerp(-dpf2(42 + 21), nameX, diff);
+                onlineX = AndroidUtilities.lerp(-dpf2(42 + 21), onlineX, diff);
+            }
+            final float kx = dpf2(8);
+            final float ky = isPulledDown ? dpf2(8) : dpf2(-24);
+
+            final float nameTextViewXEnd = AndroidUtilities.dpf2(18f) - ((FrameLayout.LayoutParams) nameTextView[1].getLayoutParams()).leftMargin;
+            final float nameTextViewYEnd = newTop + extraHeight - getActionsExtraHeight() - AndroidUtilities.dpf2(30f) - nameTextView[1].getBottom();
+            final float nameTextViewCx = kx + nameX + (nameTextViewXEnd - nameX) / 2f;
+            final float nameTextViewCy = ky + nameY + (nameTextViewYEnd - nameY) / 2f;
+            nameTextViewX = (1 - value) * (1 - value) * nameX + 2 * (1 - value) * value * nameTextViewCx + value * value * nameTextViewXEnd;
+            nameTextViewY = (1 - value) * (1 - value) * nameY + 2 * (1 - value) * value * nameTextViewCy + value * value * nameTextViewYEnd;
+
+            final float onlineTextViewXEnd = AndroidUtilities.dpf2(16f) - ((FrameLayout.LayoutParams) onlineTextView[1].getLayoutParams()).leftMargin;
+            final float onlineTextViewYEnd = newTop + extraHeight - getActionsExtraHeight() - AndroidUtilities.dpf2(10f) - onlineTextView[1].getBottom();
+            final float onlineTextViewCx = kx + onlineX + (onlineTextViewXEnd - onlineX) / 2f;
+            final float onlineTextViewCy = ky + onlineY + (onlineTextViewYEnd - onlineY) / 2f;
+            onlineTextViewX = (1 - value) * (1 - value) * onlineX + 2 * (1 - value) * value * onlineTextViewCx + value * value * onlineTextViewXEnd;
+            onlineTextViewY = (1 - value) * (1 - value) * onlineY + 2 * (1 - value) * value * onlineTextViewCy + value * value * onlineTextViewYEnd;
+
+            final float minEndNameY = (actionBar.getOccupyStatusBar() ? AndroidUtilities.statusBarHeight : 0)
+                    + ActionBar.getCurrentActionBarHeight() / 2f
+                    - 21 * AndroidUtilities.density
+                    + actionBar.getTranslationY();
 
-        final float minNameY = (float) Math.floor(minEndNameY) + AndroidUtilities.dp(1.3f);
-        final float minOnlineY = minNameY + AndroidUtilities.dpf2(22.7f);
+            final float minNameY = (float) Math.floor(minEndNameY) + AndroidUtilities.dp(1.3f);
+            final float minOnlineY = minNameY + AndroidUtilities.dpf2(22.7f);
 
-        nameTextView[1].setTranslationX(nameTextViewX);
-        nameTextView[1].setTranslationY(Math.max(minNameY, nameTextViewY));
-        onlineTextView[1].setTranslationX(getOnlineTextViewTranslationXWithOffsets(onlineTextViewX));
-        onlineTextView[1].setTranslationY(getOnlineTextViewTranslationYWithOffsets(Math.max(minOnlineY, onlineTextViewY)));
-        mediaCounterTextView.setTranslationX(onlineTextViewX);
-        mediaCounterTextView.setTranslationY(Math.max(minOnlineY, onlineTextViewY));
+            nameTextView[1].setTranslationX(nameTextViewX);
+            nameTextView[1].setTranslationY(Math.max(minNameY, nameTextViewY));
+            onlineTextView[1].setTranslationX(getOnlineTextViewTranslationXWithOffsets(onlineTextViewX));
+            onlineTextView[1].setTranslationY(getOnlineTextViewTranslationYWithOffsets(Math.max(minOnlineY, onlineTextViewY)));
+            mediaCounterTextView.setTranslationX(onlineTextViewX);
+            mediaCounterTextView.setTranslationY(Math.max(minOnlineY, onlineTextViewY));
+        }
 
         updateActionsPosition();
         updateMusicPosition();
@@ -6404,8 +6462,13 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         onlineTextView[1].setTextColor(ColorUtils.blendARGB(applyPeerColor(statusColor, true, online), 0xB3FFFFFF, value));
         if (extraHeight > getHeaderOnlyExtraHeight()) {
             nameTextView[1].setPivotY(AndroidUtilities.lerp(0, nameTextView[1].getMeasuredHeight(), value));
-            nameTextView[1].setScaleX(AndroidUtilities.lerp(1f + 0.12f * diff, 1.38f, value));
-            nameTextView[1].setScaleY(AndroidUtilities.lerp(1f + 0.12f * diff, 1.38f, value));
+            if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+                nameTextView[1].setScaleX(AndroidUtilities.lerp(1.12f, 1.67f, value));
+                nameTextView[1].setScaleY(AndroidUtilities.lerp(1.12f, 1.67f, value));
+            } else {
+                nameTextView[1].setScaleX(AndroidUtilities.lerp(1f + 0.12f * diff, 1.38f, value));
+                nameTextView[1].setScaleY(AndroidUtilities.lerp(1f + 0.12f * diff, 1.38f, value));
+            }
         }
         needLayoutText(Math.min(1f, diff));
 
@@ -6419,12 +6482,22 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
 
         avatarImage.setForegroundAlpha(value);
 
-        final FrameLayout.LayoutParams params = (FrameLayout.LayoutParams) avatarContainer.getLayoutParams();
-        params.width = (int) lerp(dpf2(100), listView.getMeasuredWidth() / avatarScale, value);
-        params.height = (int) lerp(dpf2(100), (extraHeight + newTop) / avatarScale, value);
-        params.leftMargin = (int) lerp(dpf2(64f), 0f, value);
-        fixAvatarImageInCenter();
-        avatarContainer.requestLayout();
+        if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+            // 11.14: animate translationX (avatarX -> 0) and lerp lp from 42dp.
+            avatarContainer.setTranslationX(AndroidUtilities.lerp(avatarX, 0f, value));
+            final FrameLayout.LayoutParams params = (FrameLayout.LayoutParams) avatarContainer.getLayoutParams();
+            params.width = (int) AndroidUtilities.lerp(AndroidUtilities.dpf2(42f), listView.getMeasuredWidth() / avatarScale, value);
+            params.height = (int) AndroidUtilities.lerp(AndroidUtilities.dpf2(42f), (extraHeight + newTop) / avatarScale, value);
+            params.leftMargin = (int) AndroidUtilities.lerp(AndroidUtilities.dpf2(64f), 0f, value);
+            avatarContainer.requestLayout();
+        } else {
+            final FrameLayout.LayoutParams params = (FrameLayout.LayoutParams) avatarContainer.getLayoutParams();
+            params.width = (int) lerp(dpf2(100), listView.getMeasuredWidth() / avatarScale, value);
+            params.height = (int) lerp(dpf2(100), (extraHeight + newTop) / avatarScale, value);
+            params.leftMargin = (int) lerp(dpf2(64f), 0f, value);
+            fixAvatarImageInCenter();
+            avatarContainer.requestLayout();
+        }
 
         if (giftsView != null) {
             giftsView.setExpandProgress(value);
@@ -8328,7 +8401,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         if (writeButton != null && writeButton.getVisibility() != View.GONE) {
             writeButton.setTranslationY((actionBar.getOccupyStatusBar() ? AndroidUtilities.statusBarHeight : 0) + ActionBar.getCurrentActionBarHeight() + extraHeight + searchTransitionOffset - AndroidUtilities.dp(29.5f));
 
-            writeButtonVisible = diff > 0.2f && !searchMode && !myProfile && (imageUpdater == null || setAvatarRow == -1);
+            writeButtonVisible = diff > 0.2f && !searchMode && (desu.inugram.InuConfig.OLD_LAYOUT.getValue() || !myProfile) && (imageUpdater == null || setAvatarRow == -1);
             if (writeButtonVisible && chatId != 0) {
                 writeButtonVisible = ChatObject.isChannel(currentChat) && !currentChat.megagroup && chatInfo != null && chatInfo.linked_chat_id != 0 && (infoHeaderRow != -1 || infoHeaderRowEmpty != -1);
             }
@@ -8389,6 +8462,10 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     }
 
     private void needLayout(boolean animated) {
+        if (desu.inugram.InuConfig.OLD_LAYOUT.getValue()) {
+            desu.inugram.helpers.OldProfileHelper.needLayout(this, animated);
+            return;
+        }
         final int newTop = (actionBar.getOccupyStatusBar() ? AndroidUtilities.statusBarHeight : 0) + ActionBar.getCurrentActionBarHeight();
         final float diff = calculateHeaderExtraDiff();
         boolean textMeasured = false;
@@ -8824,7 +8901,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         ProfileHelper.applyReduceMotionAlpha(openAnimationInProgress, diff, avatarContainer, giftsView, storyView);
     }
 
-    private void updateExtraViews(float newTop) {
+    public void updateExtraViews(float newTop) {
         if (isPulledDown || (overlaysView != null && overlaysView.animator != null && overlaysView.animator.isRunning())) {
             final ViewGroup.LayoutParams overlaysLp = overlaysView.getLayoutParams();
             overlaysLp.width = listView.getMeasuredWidth();
@@ -8881,7 +8958,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         avatarImage.setAlpha(1f);
     }
 
-    private void setForegroundImage(boolean secondParent) {
+    public void setForegroundImage(boolean secondParent) {
         Drawable drawable = avatarImage.getImageReceiver().getDrawable();
         if (drawable instanceof VectorAvatarThumbDrawable) {
             avatarImage.setForegroundImage(null, null, drawable);
@@ -8942,7 +9019,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         return listView;
     }
 
-    private void needLayoutText(float diff) {
+    public void needLayoutText(float diff) {
         needLayoutText(diff, 0, true);
     }
 
@@ -15809,14 +15886,14 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
     }
 
     private float lastOnlineTextViewX;
-    private float getOnlineTextViewTranslationXWithOffsets(float onlineX) {
+    public float getOnlineTextViewTranslationXWithOffsets(float onlineX) {
         lastOnlineTextViewX = onlineX;
         final float expanded = Utilities.clamp01(playProfileAnimation == 2 ? 1 : avatarAnimationProgress >= 1 || playProfileAnimation == 0 ? currentExpandAnimatorValue : 0);
         return onlineX + Math.max(0, (customPhotoOffset + getRatingViewTranslationXOffset()) * expanded);
     }
 
     private float lastOnlineTextViewY;
-    private float getOnlineTextViewTranslationYWithOffsets(float onlineY) {
+    public float getOnlineTextViewTranslationYWithOffsets(float onlineY) {
         lastOnlineTextViewY = onlineY;
         final float expanded = Utilities.clamp01(playProfileAnimation == 2 ? 1 : avatarAnimationProgress >= 1 || playProfileAnimation == 0 ? currentExpandAnimatorValue : 0);
         return onlineY + getRatingViewTranslationYOffset() * (1.0f - expanded);
@@ -15982,7 +16059,7 @@ public class ProfileActivity extends BaseFragment implements NotificationCenter.
         blurredView.setVisibility(View.VISIBLE);
     }
 
-    private ShowDrawable showStatusButton;
+    public ShowDrawable showStatusButton;
 
     public ShowDrawable getShowStatusButton() {
         if (showStatusButton == null) {
