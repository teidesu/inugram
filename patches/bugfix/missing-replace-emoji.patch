From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: alina sireneva <alina@tei.su>
Date: Sun, 19 Apr 2026 05:33:38 +0300
Subject: added some missing Emoji.replaceEmoji calls in various places

---
 .../telegram/ui/Cells/AudioPlayerCell.java    |  5 ++++-
 .../org/telegram/ui/Cells/DialogCell.java     |  1 +
 .../telegram/ui/ChannelAdminLogActivity.java  |  3 ++-
 .../java/org/telegram/ui/ChatActivity.java    | 20 +++++++++++--------
 .../ui/Components/AudioPlayerAlert.java       |  3 +++
 .../telegram/ui/Components/EditTextEmoji.java |  2 +-
 .../ui/Components/MessagePreviewView.java     |  4 +++-
 .../org/telegram/ui/MainTabsActivity.java     |  4 +++-
 .../java/org/telegram/ui/MessageSeenView.java |  7 +++++--
 .../org/telegram/ui/SettingsActivity.java     |  6 ++++--
 .../recorder/CaptionContainerView.java        | 13 ++++++++++++
 11 files changed, 51 insertions(+), 17 deletions(-)

diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/AudioPlayerCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/AudioPlayerCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/AudioPlayerCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/AudioPlayerCell.java
@@ -30,12 +30,14 @@ import android.widget.ImageView;
 
 import org.telegram.messenger.AndroidUtilities;
 import org.telegram.messenger.DownloadController;
+import org.telegram.messenger.Emoji;
 import org.telegram.messenger.FileLoader;
 import org.telegram.messenger.FileLog;
 import org.telegram.messenger.ImageLoader;
 import org.telegram.messenger.LocaleController;
 import org.telegram.messenger.MediaController;
 import org.telegram.messenger.MessageObject;
+import org.telegram.messenger.NotificationCenter;
 import org.telegram.messenger.R;
 import org.telegram.messenger.SharedConfig;
 import org.telegram.messenger.UserConfig;
@@ -93,6 +95,7 @@ public class AudioPlayerCell extends FrameLayout implements DownloadController.F
         this.setWillNotDraw(false);
         this.resourcesProvider = resourcesProvider;
         this.viewType = viewType;
+        NotificationCenter.listenEmojiLoading(this);
 
         optionsButton = new ImageView(context);
         optionsButton.setScaleType(ImageView.ScaleType.CENTER);
@@ -144,7 +147,7 @@ public class AudioPlayerCell extends FrameLayout implements DownloadController.F
             CharSequence authorFinal = TextUtils.ellipsize(author, Theme.chat_contextResult_descriptionTextPaint, maxWidth, TextUtils.TruncateAt.END);
             CharSequence authorFinalH = AndroidUtilities.highlightText(authorFinal, currentMessageObject.highlightedWords, resourcesProvider);
             if (authorFinalH == null) authorFinalH = authorFinal;
-            descriptionLayout = new StaticLayout(authorFinalH, Theme.chat_contextResult_descriptionTextPaint, maxWidth + dp(4), Layout.Alignment.ALIGN_NORMAL, 1.0f, 0.0f, false);
+            descriptionLayout = new StaticLayout(Emoji.replaceEmoji(authorFinalH, Theme.chat_contextResult_descriptionTextPaint.getFontMetricsInt(), false), Theme.chat_contextResult_descriptionTextPaint, maxWidth + dp(4), Layout.Alignment.ALIGN_NORMAL, 1.0f, 0.0f, false);
             descriptionLayoutEmojis = AnimatedEmojiSpan.update(AnimatedEmojiDrawable.CACHE_TYPE_MESSAGES, this, descriptionLayoutEmojis, descriptionLayout);
         } catch (Exception e) {
             FileLog.e(e);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java b/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Cells/DialogCell.java
@@ -2525,6 +2525,7 @@ public class DialogCell extends BaseCell implements StoriesListPlaceProvider.Ava
             }
         } else if ((useForceThreeLines || SharedConfig.useThreeLinesLayout) && !hasTags() && messageNameString != null && (currentDialogFolderId == 0 || currentDialogFolderDialogsCount == 1)) {
             try {
+                messageNameString = Emoji.replaceEmoji(messageNameString, Theme.dialogs_messageNamePaint.getFontMetricsInt(), false);
                 if (message != null && message.hasHighlightedWords()) {
                     CharSequence s = AndroidUtilities.highlightText(messageNameString, message.highlightedWords, resourcesProvider);
                     if (s != null) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChannelAdminLogActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChannelAdminLogActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChannelAdminLogActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChannelAdminLogActivity.java
@@ -84,6 +84,7 @@ import org.telegram.messenger.BuildVars;
 import org.telegram.messenger.ChatObject;
 import org.telegram.messenger.ContactsController;
 import org.telegram.messenger.DialogObject;
+import org.telegram.messenger.Emoji;
 import org.telegram.messenger.FileLoader;
 import org.telegram.messenger.FileLog;
 import org.telegram.messenger.ImageLocation;
@@ -688,7 +689,7 @@ public class ChannelAdminLogActivity extends BaseFragment implements Notificatio
             ssb.append(" S");
             ssb.setSpan(new ColoredImageSpan(drawable), ssb.length() - 1, ssb.length(), Spanned.SPAN_EXCLUSIVE_EXCLUSIVE);
         }
-        messageObject.messageText = ssb;
+        messageObject.messageText = Emoji.replaceEmoji(ssb, Theme.chat_msgTextPaint.getFontMetricsInt(), false);
         MessageObject lastMessage = messages.size() <= 0 ? null : messages.get(messages.size() - 1);
         if (lastMessage != null) {
             if (!stableIdByEventExpand.containsKey(lastMessage.eventId))
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/ChatActivity.java
@@ -15117,11 +15117,11 @@ public class ChatActivity extends BaseFragment implements
                             }
                         }
                         if (userNamesCount == 1) {
-                            mess = TextUtils.concat(userNames, ": ", mess);
+                            mess = TextUtils.concat(Emoji.replaceEmoji(userNames, replyObjectTextView.getPaint().getFontMetricsInt(), false), ": ", mess);
                         }
                         replyObjectTextView.setText(mess);
                     } else {
-                        replyObjectTextView.setText(LocaleController.formatString("ForwardingFromNames", R.string.ForwardingFromNames, userNames));
+                        replyObjectTextView.setText(Emoji.replaceEmoji(LocaleController.formatString("ForwardingFromNames", R.string.ForwardingFromNames, userNames), replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     }
                 }
                 replyObjectHintTextView.setText(LocaleController.getString(R.string.TapForForwardingOptions));
@@ -15144,20 +15144,20 @@ public class ChatActivity extends BaseFragment implements
                     replyObjectTextView.setText(pendingLinkSearchString);
                 } else {
                     if (webPage.site_name != null) {
-                        replyNameTextView.setText(webPage.site_name);
+                        replyNameTextView.setText(Emoji.replaceEmoji(webPage.site_name, replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     } else if (webPage.title != null) {
-                        replyNameTextView.setText(webPage.title);
+                        replyNameTextView.setText(Emoji.replaceEmoji(webPage.title, replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     } else {
                         replyNameTextView.setText(LocaleController.getString(R.string.LinkPreview));
                     }
                     if (webPage.title != null) {
-                        replyObjectTextView.setText(webPage.title);
+                        replyObjectTextView.setText(Emoji.replaceEmoji(webPage.title, replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     } else if (webPage.description != null) {
-                        replyObjectTextView.setText(webPage.description);
+                        replyObjectTextView.setText(Emoji.replaceEmoji(webPage.description, replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     } else if (webPage.author != null) {
-                        replyObjectTextView.setText(webPage.author);
+                        replyObjectTextView.setText(Emoji.replaceEmoji(webPage.author, replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     } else {
-                        replyObjectTextView.setText(webPage.display_url);
+                        replyObjectTextView.setText(Emoji.replaceEmoji(webPage.display_url, replyObjectTextView.getPaint().getFontMetricsInt(), false));
                     }
                     chatActivityEnterView.setWebPage(webPage, true);
                 }
@@ -21464,6 +21464,9 @@ public class ChatActivity extends BaseFragment implements
             if (replyObjectTextView != null) {
                 replyObjectTextView.invalidate();
             }
+            if (replyNameTextView != null) {
+                replyNameTextView.invalidate();
+            }
             if (alertTextView != null) {
                 alertTextView.invalidate();
             }
@@ -27953,6 +27956,7 @@ public class ChatActivity extends BaseFragment implements
                 pinnedMessageObject.updateTranslation(false);
                 if (pinnedMessageObject.type == MessageObject.TYPE_MUSIC) {
                     pinnedText = String.format("%s - %s", pinnedMessageObject.getMusicAuthor(), pinnedMessageObject.getMusicTitle());
+                    pinnedText = Emoji.replaceEmoji(pinnedText, messageTextView.getPaint().getFontMetricsInt(), false);
                 } else if (pinnedMessageObject.type == MessageObject.TYPE_POLL) {
                     if (pinnedMessageObject.messageOwner.media instanceof TLRPC.TL_messageMediaPoll) {
                         TLRPC.TL_messageMediaPoll poll = (TLRPC.TL_messageMediaPoll) pinnedMessageObject.messageOwner.media;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/AudioPlayerAlert.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/AudioPlayerAlert.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/AudioPlayerAlert.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/AudioPlayerAlert.java
@@ -76,6 +76,7 @@ import org.telegram.messenger.BuildVars;
 import org.telegram.messenger.ContactsController;
 import org.telegram.messenger.DialogObject;
 import org.telegram.messenger.DownloadController;
+import org.telegram.messenger.Emoji;
 import org.telegram.messenger.FileLoader;
 import org.telegram.messenger.FileLog;
 import org.telegram.messenger.FileRefController;
@@ -3334,6 +3335,7 @@ public class AudioPlayerAlert extends BottomSheet implements NotificationCenter.
                     textViews[i].setAlpha(0f);
                     textViews[i].setVisibility(GONE);
                 }
+                NotificationCenter.listenEmojiLoading(textViews[i]);
                 addView(textViews[i], LayoutHelper.createFrame(LayoutHelper.WRAP_CONTENT, LayoutHelper.MATCH_PARENT));
             }
             gradientMatrix = new Matrix();
@@ -3437,6 +3439,7 @@ public class AudioPlayerAlert extends BottomSheet implements NotificationCenter.
         }
 
         public void setText(CharSequence text, boolean animated) {
+            text = Emoji.replaceEmoji(text, textViews[activeIndex].getPaint().getFontMetricsInt(), false);
             final CharSequence currentText = textViews[activeIndex].getText();
 
             if (TextUtils.isEmpty(currentText) || !animated) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/EditTextEmoji.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/EditTextEmoji.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/EditTextEmoji.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/EditTextEmoji.java
@@ -74,7 +74,7 @@ public class EditTextEmoji extends FrameLayout implements NotificationCenter.Not
     private boolean showKeyboardOnResume;
     private int lastSizeChangeValue1;
     private boolean lastSizeChangeValue2;
-    private int innerTextChange;
+    public int innerTextChange;
     private boolean allowAnimatedEmoji;
     public boolean includeNavigationBar;
     AdjustPanLayoutHelper adjustPanLayoutHelper;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Components/MessagePreviewView.java b/TMessagesProj/src/main/java/org/telegram/ui/Components/MessagePreviewView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Components/MessagePreviewView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Components/MessagePreviewView.java
@@ -45,6 +45,7 @@ import org.telegram.messenger.BuildVars;
 import org.telegram.messenger.ChatMessageSharedResources;
 import org.telegram.messenger.ChatObject;
 import org.telegram.messenger.ContactsController;
+import org.telegram.messenger.Emoji;
 import org.telegram.messenger.FileLog;
 import org.telegram.messenger.MessagePreviewParams;
 import org.telegram.messenger.LocaleController;
@@ -2144,6 +2145,7 @@ public class MessagePreviewView extends FrameLayout {
             subtitle.setEllipsizeByGradient(!LocaleController.isRTL);
             subtitle.setCallback(this);
             subtitle.setOverrideFullWidth(AndroidUtilities.displaySize.x);
+            NotificationCenter.listenEmojiLoading(this);
         }
 
         public void setAnimationForward(boolean forward) {
@@ -2156,7 +2158,7 @@ public class MessagePreviewView extends FrameLayout {
         }
 
         public void setSubtitle(CharSequence text, boolean animated) {
-            subtitle.setText(text, animated && !LocaleController.isRTL);
+            subtitle.setText(Emoji.replaceEmoji(text, subtitle.getPaint().getFontMetricsInt(), false), animated && !LocaleController.isRTL);
         }
 
         @Override
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/MainTabsActivity.java
@@ -32,6 +32,7 @@ import org.telegram.messenger.AndroidUtilities;
 import org.telegram.messenger.ApplicationLoader;
 import org.telegram.messenger.BuildConfig;
 import org.telegram.messenger.ContactsController;
+import org.telegram.messenger.Emoji;
 import org.telegram.messenger.FileLoader;
 import org.telegram.messenger.LiteMode;
 import org.telegram.messenger.LocaleController;
@@ -479,9 +480,10 @@ public class MainTabsActivity extends ViewPagerActivity implements NotificationC
         final TextView textView = new TextView(getContext());
         textView.setTextSize(TypedValue.COMPLEX_UNIT_DIP, 16);
         textView.setTextColor(getThemedColor(Theme.key_dialogTextBlack));
-        textView.setText(UserObject.getUserName(user));
+        textView.setText(Emoji.replaceEmoji(UserObject.getUserName(user), textView.getPaint().getFontMetricsInt(), false));
         textView.setMaxLines(2);
         textView.setEllipsize(TextUtils.TruncateAt.END);
+        NotificationCenter.listenEmojiLoading(textView);
         btn.addView(textView, LayoutHelper.createLinear(0, LayoutHelper.WRAP_CONTENT, 1f, Gravity.CENTER_VERTICAL, 13, 0, 14, 0));
 
         return btn;
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/MessageSeenView.java b/TMessagesProj/src/main/java/org/telegram/ui/MessageSeenView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/MessageSeenView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/MessageSeenView.java
@@ -24,6 +24,7 @@ import androidx.recyclerview.widget.RecyclerView;
 import org.telegram.messenger.AndroidUtilities;
 import org.telegram.messenger.ChatObject;
 import org.telegram.messenger.ContactsController;
+import org.telegram.messenger.Emoji;
 import org.telegram.messenger.ImageLocation;
 import org.telegram.messenger.LocaleController;
 import org.telegram.messenger.MessageObject;
@@ -78,6 +79,7 @@ public class MessageSeenView extends FrameLayout {
         titleView.setTextSize(16);
         titleView.setEllipsizeByGradient(true);
         titleView.setRightPadding(AndroidUtilities.dp(62));
+        NotificationCenter.listenEmojiLoading(titleView);
 
         addView(titleView, LayoutHelper.createFrame(0, LayoutHelper.WRAP_CONTENT, Gravity.LEFT | Gravity.CENTER_VERTICAL, 40, 0, 0, 0));
 
@@ -269,7 +271,7 @@ public class MessageSeenView extends FrameLayout {
 
         avatarsImageView.commitTransition(false);
         if (peerIds.size() == 1 && users.get(0) != null) {
-            titleView.setText(ContactsController.formatName(users.get(0)));
+            titleView.setText(Emoji.replaceEmoji(ContactsController.formatName(users.get(0)), titleView.getPaint().getFontMetricsInt(), false));
         } else {
             if (peerIds.size() == 0) {
                 titleView.setText(LocaleController.getString(R.string.NobodyViewed));
@@ -375,6 +377,7 @@ public class MessageSeenView extends FrameLayout {
             nameView.setImportantForAccessibility(View.IMPORTANT_FOR_ACCESSIBILITY_NO);
             nameView.setTextColor(Theme.getColor(Theme.key_actionBarDefaultSubmenuItem));
             nameView.setGravity(LocaleController.isRTL ? Gravity.RIGHT : Gravity.LEFT);
+            NotificationCenter.listenEmojiLoading(nameView);
 
             statusBadgeComponent = new StatusBadgeComponent(this);
             nameView.setDrawablePadding(AndroidUtilities.dp(3));
@@ -415,7 +418,7 @@ public class MessageSeenView extends FrameLayout {
                 avatarDrawable.setInfo(currentAccount, object);
                 ImageLocation imageLocation = ImageLocation.getForUserOrChat(object, ImageLocation.TYPE_SMALL);
                 avatarImageView.setImage(imageLocation, "50_50", avatarDrawable, object);
-                nameView.setText(ContactsController.formatName(object));
+                nameView.setText(Emoji.replaceEmoji(ContactsController.formatName(object), nameView.getPaint().getFontMetricsInt(), false));
             }
 
             if (date <= 0) {
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java b/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/SettingsActivity.java
@@ -451,6 +451,7 @@ public class SettingsActivity extends BaseFragment implements NotificationCenter
         titleView.setGravity(Gravity.CENTER);
         titleView.setSingleLine();
         titleView.setEllipsize(TextUtils.TruncateAt.END);
+        NotificationCenter.listenEmojiLoading(titleView);
         topView.addView(titleView, LayoutHelper.createFrame(LayoutHelper.MATCH_PARENT, LayoutHelper.WRAP_CONTENT, Gravity.CENTER_HORIZONTAL | Gravity.TOP, 16, 138.333f - 12, 16, 0));
 
         subtitleView = new TextView(context);
@@ -527,7 +528,7 @@ public class SettingsActivity extends BaseFragment implements NotificationCenter
 
         avatarDrawable.setInfo(user);
         avatarView.setForUserOrChat(user, avatarDrawable);
-        titleView.setText(UserObject.getUserName(user));
+        titleView.setText(Emoji.replaceEmoji(UserObject.getUserName(user), titleView.getPaint().getFontMetricsInt(), false));
         final StringBuilder sb = new StringBuilder();
         if (user != null) {
             sb.append(PhoneFormat.getInstance().format("+" + user.phone));
@@ -950,6 +951,7 @@ public class SettingsActivity extends BaseFragment implements NotificationCenter
             textView.setTextSize(15);
             textView.setTypeface(AndroidUtilities.bold());
             textView.setTextColor(Theme.getColor(Theme.key_windowBackgroundWhiteBlackText, resourcesProvider));
+            NotificationCenter.listenEmojiLoading(textView);
 
             botDrawable = new AnimatedEmojiDrawable.SwapAnimatedEmojiDrawable(textView, dp(24), AnimatedEmojiDrawable.CACHE_TYPE_EMOJI_STATUS);
             emojiStatusDrawable = new AnimatedEmojiDrawable.SwapAnimatedEmojiDrawable(textView, dp(24), AnimatedEmojiDrawable.CACHE_TYPE_EMOJI_STATUS);
@@ -1012,7 +1014,7 @@ public class SettingsActivity extends BaseFragment implements NotificationCenter
             avatarDrawable.setInfo(account, user);
             avatarView.getImageReceiver().setCurrentAccount(account);
             avatarView.setForUserOrChat(user, avatarDrawable);
-            textView.setText(UserObject.getUserName(user));
+            textView.setText(Emoji.replaceEmoji(UserObject.getUserName(user), textView.getPaint().getFontMetricsInt(), false));
 
             botDrawable.setCurrentAccount(account);
             emojiStatusDrawable.setCurrentAccount(account);
diff --git a/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/CaptionContainerView.java b/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/CaptionContainerView.java
index 0000000..0000000 100644
--- a/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/CaptionContainerView.java
+++ b/TMessagesProj/src/main/java/org/telegram/ui/Stories/recorder/CaptionContainerView.java
@@ -33,6 +33,7 @@ import android.text.Editable;
 import android.text.SpannableStringBuilder;
 import android.text.TextPaint;
 import android.text.TextWatcher;
+import android.text.style.ImageSpan;
 import android.view.Gravity;
 import android.view.MotionEvent;
 import android.view.RoundedCorner;
@@ -341,6 +342,9 @@ public class CaptionContainerView extends FrameLayout {
                 if (editText.getEditText().suppressOnTextChanged) {
                     return;
                 }
+                if (editText.innerTextChange != 2 && (count - before) > 1) {
+                    processChange = true;
+                }
                 if (mentionContainer == null) {
                     createMentionsContainer();
                 }
@@ -352,9 +356,18 @@ public class CaptionContainerView extends FrameLayout {
 
             private int lastLength;
             private boolean lastOverLimit;
+            private boolean processChange;
 
             @Override
             public void afterTextChanged(Editable s) {
+                if (processChange) {
+                    ImageSpan[] spans = s.getSpans(0, s.length(), ImageSpan.class);
+                    for (int i = 0; i < spans.length; i++) {
+                        s.removeSpan(spans[i]);
+                    }
+                    Emoji.replaceEmoji(s, editText.getEditText().getPaint().getFontMetricsInt(), false, null);
+                    processChange = false;
+                }
                 codePointCount = Character.codePointCount(s, 0, s.length());
                 String limitText = null;
                 final int limit = getCaptionLimit();
